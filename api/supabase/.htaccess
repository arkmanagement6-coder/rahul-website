# api/supabase/.htaccess

RewriteEngine On

# Allow direct access to index.php or files/folders
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Rewrite everything else to index.php with the query params
RewriteRule ^(.*)$ index.php?path=$1 [QSA,L]
